{
  "repository": "https://github.com/drewolson/epicbot",
  "license": "MIT",
  "scripts": {
    "test": "spago test",
    "bundle": "spago bundle-app -t dist/index.js && ncc build -m dist/index.js -o dist/bundle",
    "format": "find src test -name \"*.purs\" | xargs -n1 purty format --write",
    "format:check": "find src test -name \"*.purs\" | xargs -n1 purty validate && prettier --check src/**/*.js",
    "lint": "eslint src/"
  },
  "eslintConfig": {
    "extends": "eslint:recommended",
    "env": {
      "node": true,
      "es6": true
    }
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "elasticlunr": "^0.9.5",
    "node-fetch": "^2.6.0",
    "uuid": "^7.0.3",
    "uuid-validate": "^0.0.3"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.1",
    "eslint": "^7.2.0",
    "prettier": "^2.0.5",
    "purty": "^6.1.3"
  }
}
